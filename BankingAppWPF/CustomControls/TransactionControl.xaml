<UserControl x:Class="BankingAppWPF.CustomControls.TransactionControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="80" d:DesignWidth="500">
    <Border BorderBrush="#27496d" BorderThickness="2">
        <Grid Background="#e8f1f5" >
            <Grid.RowDefinitions>
                <RowDefinition Height="1.5*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="0.9*"/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" x:Name="TransactionType"
                       Text="TransactionType" FontSize="20"
                       FontWeight="Bold"/>
            <TextBlock Grid.Row="1" x:Name="Description"
                           Text="Description" FontSize="15"
                           Foreground="Black"/>
            <TextBlock Grid.Row="2" x:Name="Date"
                           Text="Date" FontSize="10"
                           Foreground="Black"/>
        </Grid>
    </Border>
</UserControl>